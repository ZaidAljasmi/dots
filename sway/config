# include themes
# include ~/.config/sway/themes/catppuccin-mocha

# Read `man 5 sway` for a complete reference.

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Iosevka Term normal 14

### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# My web browser
set $browser firefox
# My terminal emulator
set $term foot

# My applications launcher
# set $wofi wofi --show drun --presenter drun --fields name --matching contains --insensitive --sort-order alphabetical --allow-images --no-actions --term foot
set $fuzzel fuzzel
set $bemenu  ~/.local/bin/wmenu-desktop.sh \
    -p "Run:" \
    -l 0 \
    -i \
    --fn "Iosevka Term 18.5" \
    --fb "#222222" \
    --ff "#bbbbbb" \
    --nb "#222222" \
    --nf "#bbbbbb" \
    --tb "#285577" \
    --hb "#285577" \
    --tf "#eeeeee" \
    --hf "#eeeeee" \
    --af "#bbbbbb" \
    --ab "#222222" \
    --cb "#222222" \
    --cf "#bbbbbb"
# set $wmenu wmenu-run \
#     -p "Run:" \
#     -f "Iosevka Term normal 17" \
#     -l 0 \
#     -i \
#     -N 222222 \
#     -n bbbbbb \
#     -M 285577 \
#     -m eeeeee \
#     -S 285577 \
#     -s eeeeee
# default wmenu colors :
    # -N 222222 \
    # -n bbbbbb \
    # -M 005577 \
    # -m eeeeee \
    # -S 005577 \
    # -s eeeeee
#-------------------#

# Power menu
set $wmenu-powermenu ~/.config/sway/scripts/powermenu
set $wofi-powermenu ~/.config/wofi/powermenu
set $fuzzel-powermenu ~/.config/fuzzel/scripts/PowerMenu   

# include /etc/sway/config-vars.d/*
# include /etc/sway/config.d/*

### Output configuration
#
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
# This is commented in Debian, because the Sway wallpaper files are in a separate
# package `sway-backgrounds`. Installing this package drops a config file to
# /etc/sway/config.d/
# output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill
#
# Example configuration:
#
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs

### Idle configuration
#
# Example configuration:
#
 exec swayidle -w \
          timeout 300 'swaylock -f -c 000000' \
          timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
          
# for sleep you can use > before-sleep 'swaylock -f -c 000000'
# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.

### Input configuration
#
# Example configuration:
#
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.

# for_window [class="Spotify"] opacity 0.9
# for_window [app_id="firefox"] opacity 0.95

### Key bindings
#
# Basics:
#
    # Start a browser
    bindsym --to-code $mod+alt+b exec $browser

    # Start a terminal
    bindsym --to-code $mod+Return exec $term

    # Kill focused window
    bindsym --to-code $mod+Shift+q kill

    # Start a launcher
    bindsym --to-code $mod+p exec $fuzzel --terminal="$TERMINAL -e"
    bindsym --to-code $mod+shift+e exec $fuzzel-powermenu

    # Switch to another language
input * {
    xkb_layout "us,ara"
    xkb_options "grp:alt_space_toggle,bell:none,caps:swapescape"
    xkb_switch_layout per_window
    repeat_delay 200
    repeat_rate 40
}
# or use caps:escape
# bindsym --to-code Shift+space input * xkb_switch_layout next

# Fuzzel-Scripts
bindsym --to-code $mod+Shift+s exec /home/zaid/.config/fuzzel/scripts/FuzzelScripts
bindsym --to-code $mod+Shift+p exec /home/zaid/.config/fuzzel/scripts/screenshot
# screenshot 
# bindsym --to-code $mod+Shift+p exec FILE=~/Pictures/Screenshots/screenshot_$(date '+%Y-%m-%d %I-%M-%S %p').png && grim "$FILE" && wl-copy < "$FILE" && notify-send -t 3000 -i "$FILE" "Screenshot" "Saved to: $FILE"
# screenshot with selection
# bindsym --to-code $mod+Shift+g exec FILE=~/Pictures/Screenshots/screenshot_$(date '+%Y-%m-%d %I-%M-%S %p').png && grim -g "$(slurp)" "$FILE" && wl-copy < "$FILE" && notify-send -t 3000 -i "$FILE" "Screenshot" "saved to: $FILE"

# lock
bindsym --to-code $mod+shift+D exec swaylock -C ~/.config/swaylock/config

# copy color
bindsym --to-code $mod+c exec grim -g "$(slurp -p)" -t ppm - | magick - -format "#%[hex:p{0,0}]" info:- | wl-copy

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Reload the configuration file
    bindsym --to-code $mod+Shift+c reload

    # Exit sway (logs you out of your Wayland session)
    # bindsym --to-code $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'
#
# Moving around:
#
    # Move your focus around
    bindsym --to-code $mod+$left focus left
    bindsym --to-code $mod+$down focus down
    bindsym --to-code $mod+$up focus up
    bindsym --to-code $mod+$right focus right
    # Or use $mod+[up|down|left|right]
    bindsym --to-code $mod+Left focus left
    bindsym --to-code $mod+Down focus down
    bindsym --to-code $mod+Up focus up
    bindsym --to-code $mod+Right focus right

    # Move the focused window with the same, but add Shift
    bindsym --to-code $mod+Shift+$left move left
    bindsym --to-code $mod+Shift+$down move down
    bindsym --to-code $mod+Shift+$up move up
    bindsym --to-code $mod+Shift+$right move right
    # Ditto, with arrow keys
    bindsym --to-code $mod+Shift+Left move left
    bindsym --to-code $mod+Shift+Down move down
    bindsym --to-code $mod+Shift+Up move up
    bindsym --to-code $mod+Shift+Right move right
#
# Workspaces:
#
    # Switch to workspace
    bindsym --to-code $mod+1 workspace number 1
    bindsym --to-code $mod+2 workspace number 2
    bindsym --to-code $mod+3 workspace number 3
    bindsym --to-code $mod+4 workspace number 4
    bindsym --to-code $mod+5 workspace number 5
    bindsym --to-code $mod+6 workspace number 6
    bindsym --to-code $mod+7 workspace number 7
    bindsym --to-code $mod+8 workspace number 8
    bindsym --to-code $mod+9 workspace number 9
    bindsym --to-code $mod+0 workspace number 10

    # Switch to previous/next workspace using $mod + Alt + Arrow keys
    bindsym --to-code $mod+Mod1+Left  workspace prev
    bindsym --to-code $mod+Mod1+Right workspace next
    bindsym --to-code $mod+Mod1+$left  workspace prev
    bindsym --to-code $mod+Mod1+$right workspace next


    # Move focused container to workspace
    bindsym --to-code $mod+Shift+1 move container to workspace number 1
    bindsym --to-code $mod+Shift+2 move container to workspace number 2
    bindsym --to-code $mod+Shift+3 move container to workspace number 3
    bindsym --to-code $mod+Shift+4 move container to workspace number 4
    bindsym --to-code $mod+Shift+5 move container to workspace number 5
    bindsym --to-code $mod+Shift+6 move container to workspace number 6
    bindsym --to-code $mod+Shift+7 move container to workspace number 7
    bindsym --to-code $mod+Shift+8 move container to workspace number 8
    bindsym --to-code $mod+Shift+9 move container to workspace number 9
    bindsym --to-code $mod+Shift+0 move container to workspace number 10
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym --to-code $mod+b splith
    bindsym --to-code $mod+v splitv

    # Switch the current container between different layout styles
    bindsym --to-code $mod+s layout stacking
    bindsym --to-code $mod+w layout tabbed
    bindsym --to-code $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym --to-code $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym --to-code $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym --to-code $mod+space focus mode_toggle

    # Move focus to the parent container
    bindsym --to-code $mod+a focus parent

#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym --to-code $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym --to-code $mod+minus scratchpad show
#
# Resizing containers:
#
mode "Resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym --to-code $left resize shrink width 15px
    bindsym --to-code $down resize grow height 15px
    bindsym --to-code $up resize shrink height 15px
    bindsym --to-code $right resize grow width 15px

    # Ditto, with arrow keys
    bindsym --to-code Left resize shrink width 15px
    bindsym --to-code Down resize grow height 15px
    bindsym --to-code Up resize shrink height 15px
    bindsym --to-code Right resize grow width 15px

    # Return to default mode
    bindsym --to-code Return mode "default"
    bindsym --to-code Escape mode "default"
}
bindsym --to-code $mod+r mode "Resize"

# Resize with Alt + Shift + Vim keys
bindsym --to-code Mod1+Shift+$left  resize shrink width 70px
bindsym --to-code Mod1+Shift+$down  resize grow height 70px
bindsym --to-code Mod1+Shift+$up    resize shrink height 70px
bindsym --to-code Mod1+Shift+$right resize grow width 70px
bindsym --to-code Mod1+Shift+left  resize shrink width 70px
bindsym --to-code Mod1+Shift+down  resize grow height 70px
bindsym --to-code Mod1+Shift+up    resize shrink height 70px
bindsym --to-code Mod1+Shift+right resize grow width 70px

#
# Utilities:
#
    # Special keys to adjust volume via PulseAudio
    # bindsym --to-code --locked XF86AudioMute exec pactl set-sink-mute \@DEFAULT_SINK@ toggle
    # bindsym --to-code --locked XF86AudioLowerVolume exec pactl set-sink-volume \@DEFAULT_SINK@ -5%
    # bindsym --to-code --locked XF86AudioRaiseVolume exec pactl set-sink-volume \@DEFAULT_SINK@ +5%
    # bindsym --to-code --locked XF86AudioMicMute exec pactl set-source-mute \@DEFAULT_SOURCE@ toggle
    # # Special keys to adjust brightness via brightnessctl
    # bindsym --to-code --locked XF86MonBrightnessDown exec brightnessctl set 5%-
    # bindsym --to-code --locked XF86MonBrightnessUp exec brightnessctl set 5%+
    # # Special key to take a screenshot with grim
    # bindsym --to-code Print exec grim
bindsym --to-code --locked XF86AudioMute exec sh -c 'pactl set-sink-mute @DEFAULT_SINK@ toggle; M=$(pactl get-sink-mute @DEFAULT_SINK@ | grep -o "yes\|no"); V=$(pactl get-sink-volume @DEFAULT_SINK@ | grep -o "[0-9]*%" | head -1 | tr -d "%"); if [ "$M" = "yes" ]; then notify-send -h string:x-canonical-private-synchronous:volume -h int:value:0 -t 1700 "Volume: Muted"; else notify-send -h string:x-canonical-private-synchronous:volume -h int:value:${V:-0} -t 1700 "Volume: ${V:-0}%"; fi'

bindsym --to-code --locked XF86AudioLowerVolume exec sh -c 'pactl set-sink-volume @DEFAULT_SINK@ -5%; M=$(pactl get-sink-mute @DEFAULT_SINK@ | grep -o "yes\|no"); V=$(pactl get-sink-volume @DEFAULT_SINK@ | grep -o "[0-9]*%" | head -1 | tr -d "%"); if [ "$M" = "yes" ]; then notify-send -h string:x-canonical-private-synchronous:volume -h int:value:0 -t 1700 "Volume: Muted"; else notify-send -h string:x-canonical-private-synchronous:volume -h int:value:${V:-0} -t 1700 "Volume: ${V:-0}%"; fi'

bindsym --to-code --locked XF86AudioRaiseVolume exec sh -c 'pactl set-sink-volume @DEFAULT_SINK@ +5%; M=$(pactl get-sink-mute @DEFAULT_SINK@ | grep -o "yes\|no"); V=$(pactl get-sink-volume @DEFAULT_SINK@ | grep -o "[0-9]*%" | head -1 | tr -d "%"); if [ "$M" = "yes" ]; then notify-send -h string:x-canonical-private-synchronous:volume -h int:value:0 -t 1700 "Volume: Muted"; else notify-send -h string:x-canonical-private-synchronous:volume -h int:value:${V:-0} -t 1700 "Volume: ${V:-0}%"; fi'

bindsym --to-code --locked XF86AudioMicMute exec sh -c 'pactl set-source-mute @DEFAULT_SOURCE@ toggle; M=$(pactl get-source-mute @DEFAULT_SOURCE@ | grep -o "yes\|no"); if [ "$M" = "yes" ]; then notify-send -t 1700 "Mic: Muted"; else notify-send -t 1700 "Mic: Unmuted"; fi'

bindsym --to-code --locked XF86MonBrightnessDown exec sh -c 'brightnessctl set 5%-; C=$(brightnessctl get); M=$(brightnessctl max); P=$((C*100/M)); notify-send -h string:x-canonical-private-synchronous:brightness -h int:value:$P -t 1700 "Brightness: ${P}%"'

bindsym --to-code --locked XF86MonBrightnessUp exec sh -c 'brightnessctl set +5%; C=$(brightnessctl get); M=$(brightnessctl max); P=$((C*100/M)); notify-send -h string:x-canonical-private-synchronous:brightness -h int:value:$P -t 1700 "Brightness: ${P}%"'

# bar
bar {
  font pango:Iosevka Term normal 19px
  # separator_symbol " | "
  position bottom
  swaybar_command waybar
  # status_command while date +'%a %d %b - %I:%M %p'; do sleep 1; done
  # status_command /home/zaid/.config/sway/scripts/swaybar-status
  colors {

    # default swaybar colors:
        background #000000
        statusline #ffffff
        separator  #666666

        #                  border  background  text
        focused_workspace  #4c7899  #285577  #ffffff
        active_workspace   #333333  #5f676a  #ffffff
        inactive_workspace #333333  #222222  #888888
        urgent_workspace   #2f343a  #900000  #ffffff
  }
}

# Default Sway window colors
# class                 border  backgr. text    indicator child_border
client.background        #ffffff
client.focused           #4c7899 #285577 #ffffff #2e9ef4 #285577
client.focused_inactive  #333333 #5f676a #ffffff #484e50 #5f676a
client.focused_tab_title #333333 #5f676a #ffffff
client.unfocused         #333333 #222222 #888888 #292d2e #222222
client.urgent            #2f343a #900000 #ffffff #900000 #900000
client.placeholder       #000000 #0c0c0c #ffffff #000000 #0c0c0c

# waybar toggle
bindsym --to-code $mod+o exec killall -SIGUSR1 waybar

# default_border  type none if you want no border or replace the none and type pixel 4 if you want a border (choose your favorite number)
# default_border normal 1
# for_window [floating] border normal 1
# for_window [floating] move position center
# smart_borders off

set $mode_borders Borders: (d) disable, (p) pixel, (n) normal
bindsym --to-code $mod+shift+b mode "$mode_borders"

mode "$mode_borders" {
    bindsym --to-code d border none
    bindsym --to-code p border pixel 1
    bindsym --to-code n border normal
    bindsym --to-code Return mode "default"
    bindsym --to-code Escape mode "default"
}


# gaps
# gaps inner 5
# gaps outer 1
smart_gaps off

# Press $mod+Shift+g to enter the gap mode. 
# Choose o or i for modifying outer/inner gaps.
# Press one of + / - (in-/decrement for current workspace) 
# or 0 (remove gaps for current workspace). If you also press Shift 
# with these keys, the change will be global for all workspaces.
set $mode_gaps Gaps: (o) outer, (i) inner, (d) disable, (e) enable 
set $mode_gaps_outer Gaps Outer: [+] [-]  (local) | Shift [+] [-]  (global)
set $mode_gaps_inner Gaps Inner: [+] [-]  (local) | Shift [+] [-]  (global)
bindsym --to-code $mod+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym --to-code o      mode "$mode_gaps_outer"
        bindsym --to-code i      mode "$mode_gaps_inner"
        bindsym --to-code d      gaps inner all set 0; gaps outer all set 0
        bindsym --to-code e      gaps inner all set 20; gaps outer all set 1

        bindsym --to-code Return mode "default"
        bindsym --to-code Escape mode "default"
}
mode "$mode_gaps_inner" {
        bindsym --to-code plus  gaps inner current plus 5
        bindsym --to-code minus gaps inner current minus 5
        bindsym --to-code Shift+plus  gaps inner all plus 5
        bindsym --to-code Shift+minus gaps inner all minus 5

        bindsym --to-code Return mode "default"
        bindsym --to-code Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym --to-code plus  gaps outer current plus 5
        bindsym --to-code minus gaps outer current minus 5
        bindsym --to-code Shift+plus  gaps outer all plus 5
        bindsym --to-code Shift+minus gaps outer all minus 5

        bindsym --to-code Return mode "default"
        bindsym --to-code Escape mode "default"
}

# Power Profiles
set $mode_power Power Mods: (p) Performance, (b) Balanced, (s) Saver

bindsym --to-code $mod+alt+p mode "$mode_power"

mode "$mode_power" {
    bindsym --to-code b exec "powerprofilesctl set balanced && notify-send -t 2000 'Power Profile' 'Balanced mode enabled'", mode "default"
    bindsym --to-code p exec "powerprofilesctl set performance && notify-send -t 2000 'Power Profile' 'Performance mode enabled'", mode "default"
    bindsym --to-code s exec "powerprofilesctl set power-saver && notify-send -t 2000 'Power Profile' 'Power saver mode enabled'", mode "default"

    bindsym --to-code Return mode "default"
    bindsym --to-code Escape mode "default"
}

# Power Menu
set $mode_power Power Menu: (s) Shutdown, (r) Reboot, (u) Suspend, (l) Logout
set $confirm_reboot Reboot - Are you sure? (y) Yes, (n) No
set $confirm_shutdown Shutdown - Are you sure? (y) Yes, (n) No
set $confirm_suspend Suspend - Are you sure? (y) Yes, (n) No
set $confirm_logout Logout - Are you sure? (y) Yes, (n) No

mode "$mode_power" {
    bindsym --to-code s mode "$confirm_shutdown"
    bindsym --to-code r mode "$confirm_reboot"
    bindsym --to-code u mode "$confirm_suspend"
    bindsym --to-code l mode "$confirm_logout"

    bindsym --to-code Return mode "default"
    bindsym --to-code Escape mode "default"
}

mode "$confirm_reboot" {
    bindsym --to-code y exec systemctl reboot, mode "default"
    bindsym --to-code n mode "$mode_power"
    bindsym --to-code Escape mode "default"
}

mode "$confirm_shutdown" {
    bindsym --to-code y exec systemctl poweroff, mode "default"
    bindsym --to-code n mode "$mode_power"
    bindsym --to-code Escape mode "default"
}

mode "$confirm_suspend" {
    bindsym --to-code y exec systemctl suspend, mode "default"
    bindsym --to-code n mode "$mode_power"
    bindsym --to-code Escape mode "default"
}

mode "$confirm_logout" {
    bindsym --to-code y exec pkill -KILL -u $USER, mode "default"
    bindsym --to-code n mode "$mode_power"
    bindsym --to-code Escape mode "default"
}

# bindsym --to-code $mod+Shift+m mode "$mode_power"

# autostart

# mouse speed
input "INSTANT USB GAMING MOUSE" {
    left_handed enabled
    tap enabled
    natural_scroll disabled
    dwt enabled
    accel_profile "flat"
    pointer_accel 0.1
}


#--------------------------------------------------#
    # Workspace-specific window management rules.
#--------------------------------------------------#
workspace_layout tabbed
# hide_edge_borders --i3 smart
# for_window [workspace="^([2-9]|10)$"] layout tabbed
# for_window [app_id="firefox"] move container to workspace 1

# Always float certain windows #
#swaymsg -t get_tree | grep app_id
    for_window [app_id="com.obsproject.Studio"] floating enable, resize set width 1240 px height 786 px, move position center
    for_window [app_id="pop-up"]                     floating enable
    for_window [app_id="bubble"]                     floating enable 
    for_window [app_id="task_dialog"]                floating enable
    for_window [app_id="Preferences"]                floating enable
    for_window [app_id="dialog"]                     floating enable 
    for_window [app_id="menu"]                       floating enable
    for_window [app_id="Organizer"]                  floating enable
    for_window [app_id="About"]                      floating enable
    for_window [app_id="toolbox"]                    floating enable
    for_window [app_id="page-info"]                  floating enable	
    for_window [app_id="webconsole"]                 floating enable
    for_window [app_id="Authy"]                      floating enable
    for_window [app_id="termfloat"]                  floating enable
    for_window [app_id="termfloat"]                  resize set height 540
    for_window [app_id="termfloat"]                  resize set width 960
    for_window [app_id="mpv"]                        floating enable 
    for_window [app_id="mpv"]                        resize set height 540
    for_window [app_id="mpv"]                        resize set width 960
    for_window [app_id="Nautilus"]                   floating enable 
    for_window [app_id="Nautilus"]                   resize set height 540
    for_window [app_id="Nautilus"]                   resize set width 960
    for_window [app_id="thunar" title="^Copying.*"] floating enable, move position center, resize set 600 400
    for_window [app_id="thunar" title="^Moving.*"] floating enable, move position center, resize set 600 400
    for_window [app_id="thunar" title="^Deleting.*"] floating enable, move position center, resize set 600 400
    for_window [app_id="thunar" title="^File Operation Progress"] floating enable, move position center
#-------------------------------------------------------#

#-------------------------------------------------------#
# Sticky floating windows(sticky enable|disable|toggle) #
#-------------------------------------------------------#
    for_window [app_id="danmufloat"]                  floating enable
    for_window [app_id="danmufloat"]                  sticky enable 
    for_window [app_id="danmufloat"]                  resize set height 540
    for_window [app_id="danmufloat"]                  resize set width 960
#-------------------------------------------------------#
        #           transperent settings        #
#|-------------------------------------------------------|
    # for_window [app_id="foot"] opacity 0.90
#-------------------------------------------------------#
# pre window layout script
# exec_always ~/.local/bin/sway-auto-layout

# nm-applet
# exec nm-applet --indicator
# polkit tool
exec lxpolkit 

# background
exec_always swaybg -i ~/Pictures/tokyonight/wall1.jpg -m fill

# notifications
exec_always --no-startup-id mako -c ~/.config/mako/config

# brightness
exec_always --no-startup-id brightnessctl set 25%

# dbus 
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway

# WshowKeys
# exec wshowkeys -a bottom -a right -m 50 -F "JetBrainsMono Nerd Font 25" -b "#1E1E2E" -f "#cdd6f4"
#
# for_window [app_id="wshowkeys"] {
#     floating enable
#     move position edge right bottom
#     move left 50
#     move up 50
# }

# power profiles
# exec --no-startup-id powerprofilesctl set balanced
exec --no-startup-id ~/.config/sway/scripts/Power-Profile

# set the qt5ct tool for qt libs
# set cursor theme:
# orginal cursor size 24 
# optins >  Bibata-Modern-Amber , breeze_cursors, Breeze_Light, Adwaita
# seat seat0 xcursor_theme Adwaita 24

# Notes:

# gsettings set org.gnome.desktop.interface font-name 'Sans 14' --> this command use to set font type and size
# gsettings set org.gnome.desktop.interface icon-theme 'Tokyonight-Dark' --> this command use to set icons theme for GTK apps 
# gsettings set org.gnome.desktop.interface gtk-theme 'Tokyonight-Dark' --> this command use to set GTK theme 
