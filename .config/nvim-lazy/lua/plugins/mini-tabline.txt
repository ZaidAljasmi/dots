return {
  "nvim-mini/mini.tabline",
  version = "*",
  config = function()
    require("mini.tabline").setup({
      show_icons = false,
      format = nil,
      tabpage_section = "left",
    })

    vim.api.nvim_set_hl(0, "MiniTablineCurrent",{ underline = false, bold = true })
  end,
}
